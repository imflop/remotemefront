import React from 'react';

import './Header.scss';

const Header = () => {
    return (
        <header className="header">
            This is header
        </header>
    );
}

export default Header;